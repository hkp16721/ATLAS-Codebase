How does heap sort work ? explain the technique in 5 .. algorithm

HeapSort(array):
    n = length(array)
    for i from n/2 - 1 down to 0:
        Heapify(array, n, i)
    for i from n - 1 down to 1:
        Swap(array[0], array[i])
        Heapify(array, i, 0)

Heapify(array, n, i):
    largest = i
    left = 2 * i + 1
    right = 2 * i + 2
    if left < n and array[left] > array[largest]:
        largest = left
    if right < n and array[right] > array[largest]:
        largest = right
    if largest != i:
        Swap(array[i], array[largest])
        Heapify(array, n, largest)

Swap(a, b):
    temp = a
    a = b
    b = temp